extends  base.pug

block content 
    h4 Change Title of Video
    form(method="POST")
        input(name = "title" placeholder="New Title", value=video.title, required)
        input(name = "Description" placeholder="New Description", value=video.title, required)
        input(name = "Hashtags" placeholder="Hashtags, seperated by comma.", value=video.title, required)
        input(value="Save", type="submit")